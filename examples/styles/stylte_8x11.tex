/* BuildBook - Letter/Magazine Stylesheet (8.5x11) */

/* Optimized for larger measures (longer lines) and block-style layouts.
   Focuses on readability through increased leading and generous whitespace.
*/

body {
  font-family: "Linux Libertine O", Georgia, serif;
  line-height: 1.7; /* Increased for the wide 8.5" page */
  margin: 10%;
  color: #1a1a1a;
}

/* Block Paragraphs */
p {
  margin-top: 0;
  margin-bottom: 1.2em;
  text-indent: 0;
}

/* Headings - Scaled for the Letter canvas */
h1 {
  font-size: 2.5em;
  font-weight: bold;
  text-align: left;
  margin-top: 3em;
  margin-bottom: 1em;
  border-bottom: 3px solid #000;
  padding-bottom: 10px;
}

h2 {
  font-size: 1.8em;
  font-weight: normal;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-top: 2em;
  color: #444;
}

h3 {
  font-size: 1.4em;
  font-weight: bold;
  margin-top: 1.5em;
}

/* Blockquotes & Pull Quotes */
blockquote {
  margin: 2em 0;
  padding: 1.5em;
  background-color: #f9f9f9;
  border-left: 5px solid #ddd;
  font-style: italic;
}

.pullquote {
  font-size: 1.5em;
  font-weight: bold;
  text-align: center;
  margin: 2em 10%;
  color: #333;
}

/* Custom Containers */
.center-quote {
  text-align: center;
  font-style: italic;
  margin: 2em auto;
}

.note {
  font-size: 0.9em;
  background: #eee;
  padding: 1em;
  border-radius: 4px;
}

/* Technical Elements */
code {
  font-family: "DejaVu Sans Mono", monospace;
  background: #f4f4f4;
  padding: 2px 4px;
}

pre {
  background: #f4f4f4;
  padding: 1em;
  overflow-x: auto;
  font-size: 0.9em;
}